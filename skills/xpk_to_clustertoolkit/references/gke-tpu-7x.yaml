# Copyright 2026 Google LLC. All Rights Reserved.

blueprint_name: gke-tpu-7x

vars:
  # GCP Project ID
  project_id:

  # Deployment Name
  deployment_name: gke-tpu-7x

  # GCP Region & Zone
  region:
  zone:

  # TPU Slice configuration
  num_slices:
  machine_type: tpu7x-standard-4t
  tpu_topology:
  static_node_count:
  spot: false
  enable_flex_start: false

  # Reservation
  reservation:

  # GKE Version & Maintenance
  gke_version: "1.35."
  maintenance_interval:

  # Access & Networking
  authorized_cidr:
  master_authorized_networks:
  enable_private_endpoint: false
  private_endpoint_subnetwork:
  enable_master_global_access: false
  enable_workload_identity: true

  # System Node Pool & Autoprovisioning
  system_node_pool_machine_type: "n2-standard-8"
  system_pool_node_count:
  enable_autoprovisioning: false
  autoprovisioning_min_chips:
  autoprovisioning_max_chips:

  # Pathways
  enable_pathways_for_tpus: false
  pathways_gce_machine_type: "n2-standard-64"

  # Storage CSI Drivers
  enable_gcsfuse_csi_driver: true
  enable_filestore_csi_driver: false
  enable_parallelstore_csi_driver: false
  enable_pd_csi: false

  # Checkpointing & Diagnostics
  enable_mtc: false
  mtc_ramdisk_size:
  mtc_gcs_bucket:
  mtc_toleration_key:
  enable_managed_mldiagnostics: false

deployment_groups:
- group: primary
  modules:
  - id: gke-tpu-7x-net
    source: modules/network/vpc
    settings:
      subnetworks:
      - subnet_name: $(vars.deployment_name)-sub-0
        subnet_region: $(vars.region)
        subnet_ip: 192.168.0.0/18
      secondary_ranges_list:
      - subnetwork_name: $(vars.deployment_name)-sub-0
        ranges:
        - range_name: pods
          ip_cidr_range: 10.4.0.0/14
        - range_name: services
          ip_cidr_range: 10.0.32.0/20
      firewall_rules:
      - name: $(vars.deployment_name)-internal-0
        ranges: [192.168.0.0/18]
        allow:
        - protocol: tcp
          ports: ["0-65535"]
        - protocol: udp
          ports: ["0-65535"]
        - protocol: icmp

  - id: node_pool_service_account
    source: modules/project/service-account
    settings:
      name: gke-np-sa
      project_roles:
      - logging.logWriter
      - monitoring.metricWriter
      - monitoring.viewer
      - stackdriver.resourceMetadata.writer
      - storage.objectViewer
      - artifactregistry.reader

  - id: workload_service_account
    source: modules/project/service-account
    settings:
      name: gke-wl-sa
      project_roles:
      - logging.logWriter
      - monitoring.metricWriter
      - monitoring.viewer
      - stackdriver.resourceMetadata.writer
      - storage.objectAdmin
      - artifactregistry.reader
      - container.admin

  - id: gke-tpu-7x-cluster
    source: modules/scheduler/gke-cluster
    use: [gke-tpu-7x-net, workload_service_account]
    settings:
      system_node_pool_machine_type: $(vars.system_node_pool_machine_type)
      system_node_pool_taints: []
      enable_private_endpoint: $(vars.enable_private_endpoint)
      private_endpoint_subnetwork: $(vars.private_endpoint_subnetwork)
      enable_master_global_access: $(vars.enable_master_global_access)
      enable_pathways_for_tpus: $(vars.enable_pathways_for_tpus)
      pathways_gce_machine_type: $(vars.pathways_gce_machine_type)
      enable_gcsfuse_csi: $(vars.enable_gcsfuse_csi_driver)
      enable_filestore_csi: $(vars.enable_filestore_csi_driver)
      enable_parallelstore_csi: $(vars.enable_parallelstore_csi_driver)
      enable_pd_csi: $(vars.enable_pd_csi)
      enable_autoprovisioning: $(vars.enable_autoprovisioning)
      autoprovisioning_min_chips: $(vars.autoprovisioning_min_chips)
      autoprovisioning_max_chips: $(vars.autoprovisioning_max_chips)
      maintenance_policy: $(vars.maintenance_interval)
      configure_workload_identity_sa: $(vars.enable_workload_identity)
      master_authorized_networks:
      - cidr_block: $(vars.authorized_cidr)
        display_name: "kubectl-access-network"
      version_prefix: $(vars.gke_version)
      release_channel: REGULAR
      maintenance_exclusions:
      - name: no-minor-or-node-upgrades-indefinite
        start_time: "2025-12-01T00:00:00Z"
        exclusion_scope: NO_MINOR_OR_NODE_UPGRADES
        exclusion_end_time_behavior: UNTIL_END_OF_SUPPORT
    outputs: [instructions]

  - id: workload_policy
    source: modules/compute/resource-policy
    settings:
      name: "tpu7x-workload-policy"
      project_id: $(vars.project_id)
      region: $(vars.region)
      workload_policy:
        accelerator_topology: $(vars.tpu_topology)
        type: "HIGH_THROUGHPUT"

  - id: gke-tpu-7x-pool
    source: modules/compute/gke-node-pool
    use: [gke-tpu-7x-cluster, node_pool_service_account, workload_policy]
    settings:
      num_slices: $(vars.num_slices)
      name: gke-tpu-7x-pool
      disk_type: hyperdisk-balanced
      machine_type: $(vars.machine_type)
      static_node_count: $(vars.static_node_count)
      spot: $(vars.spot)
      enable_flex_start: $(vars.enable_flex_start)
      auto_upgrade: true
      zones: [$(vars.zone)]
      reservation_affinity:
        consume_reservation_type: SPECIFIC_RESERVATION
        specific_reservations:
        - name: $(vars.reservation)
    outputs: [instructions]

  - id: workload-manager-install
    source: modules/management/kubectl-apply
    use: [gke-tpu-7x-cluster]
    settings:
      kueue:
        install: true
        config_template_vars:
          num_slices: $(vars.num_slices)
          node_count: $(gke-tpu-7x-pool.node_count_static)
          tpu_chips_per_node: $(gke-tpu-7x-pool.tpu_chips_per_node)
          accelerator_type: $(gke-tpu-7x-pool.tpu_accelerator_type)
      jobset:
        install: true
