# Copyright 2024 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

---

# See instructions at
# https://github.com/GoogleCloudPlatform/hpc-toolkit/tree/main/examples#image-builderyaml-

blueprint_name: hpc-slurm6-hybrid

vars:
  project_id: ## Set GCP Project ID Here ##

  #Key type: "munge" or "slurm". local_key is the path to your
  #existing key on the shared filesystem; override it if it lives elsewhere.
  key_type: munge
  local_key: /share/$(vars.key_type).key

  #This gets derived based on the key_type
  cloud_key_location: /etc/$(vars.key_type)/$(vars.key_type).key
  cloud_key_user: $(vars.key_type)

  #leave this variable empty to use the default gcp slurm version
  slurm_version: "25.11.6"

  deployment_name: slurm6-hybrid
  onprem_ctld_host: "your_slurmctld_name"
  onprem_ctld_addr: "your_slurmctld_IPaddres"
  cluster_name: "your-cluster-name"
  on_prem_install_dir: "your_slurm_installation_directory"
  slurm_uid: 620
  slurm_gid: 620
  output_dir: /share/$(vars.deployment_name)-files
  region: us-central1
  zone: us-central1-c

  # see latest in https://github.com/GoogleCloudPlatform/slurm-gcp/blob/master/docs/images.md#published-image-family
  latest_base_image: slurm-gcp-6-11-hpc-rocky-linux-8
  custom_image:
    family: hybrid
    project: $(vars.project_id)
  disk_size: 64

  #If the controller has a fixed IP or is always in a range specify it here
  controller_ip_range: "0.0.0.0/0"
  slurmd_port: 6818


deployment_groups:
- group: network
  modules:
  - id: network
    source: modules/network/vpc
    settings:
      network_name: "slurm-hybrid-net"
      firewall_rules:
      - name: "slurmd-ingress"
        description: "My slurmd ingress"
        direction: "INGRESS"
        ranges:
        - $(vars.controller_ip_range)
        allow:
        - protocol: "tcp"
          ports:
          - $(vars.slurmd_port)


- group: scripts_for_image
  modules:
  - id: scripts_for_image
    source: modules/scripts/startup-script
    settings:
      runners:
      - type: data
        destination: $(vars.cloud_key_location)
        source: $(vars.local_key)
      #Ensure key has the proper settings
      - type: shell
        destination: prepare_key.sh
        content: |
          #!/bin/sh
          chmod 600 $(vars.cloud_key_location)
          chown $(vars.cloud_key_user): $(vars.cloud_key_location)
      #Align the cloud slurm user/group uid/gid with the on-prem values. If the
      #target id is already taken by another account, that account is relocated
      #to a free id (and its files re-owned) first. Baked into the image so it
      #runs once at build time rather than on every node boot.
      - type: shell
        source: $(ghpc_stage("./hybrid-scripts/align_slurm_ids.sh"))
        destination: align_slurm_ids.sh
        args: "$(vars.slurm_uid) $(vars.slurm_gid)"
      #Optionally build a specific Slurm version into the image. Leave
      #vars.slurm_version empty ("") to skip; the script no-ops on an empty arg.
      - type: shell
        source: $(ghpc_stage("./hybrid-scripts/custom_slurm_version.sh"))
        destination: custom_slurm_version.sh
        args: "$(vars.slurm_version)"
      #This scripts creates on the compute node image all the necessary slurmd
      #directories as we have on-prem. For things like the log or the etc dir
      #mkdir is good enough, but with the ln we keep it all in the same place.
      - type: shell
        destination: prepare_slurm_dirs.sh
        content: |
          #!/bin/sh
          mkdir -p $(vars.on_prem_install_dir)/var/spool/slurmd
          ln -s /usr/local/etc/slurm $(vars.on_prem_install_dir)/etc
          ln -s /usr/local/bin $(vars.on_prem_install_dir)/bin
          ln -s /usr/local/sbin $(vars.on_prem_install_dir)/sbin
          ln -s /var/log/slurm $(vars.on_prem_install_dir)/var/log
          chown -R $(vars.slurm_uid):$(vars.slurm_gid) $(vars.on_prem_install_dir)

- group: image
  modules:
  - id: image-with-key
    source: modules/packer/custom-image
    kind: packer
    use:
    - network
    - scripts_for_image
    settings:
      source_image_project_id: [schedmd-slurm-public]
      source_image_family: $(vars.latest_base_image)
      disk_size: $(vars.disk_size)
      image_family: $(vars.custom_image.family)
      state_timeout: 15m

- group: cluster
  modules:
  - id: compute_nodeset
    source: community/modules/compute/schedmd-slurm-gcp-v6-nodeset
    use: [network]
    settings:
      node_count_dynamic_max: 20
      disk_size_gb: $(vars.disk_size)
      instance_image: $(vars.custom_image)
      instance_image_custom: true

  - id: compute_partition
    source: community/modules/compute/schedmd-slurm-gcp-v6-partition
    use: [compute_nodeset]
    settings:
      partition_name: compute
      is_default: true

  - id: slurm_controller
    source: community/modules/scheduler/schedmd-slurm-gcp-v6-controller
    use:
    - network
    - compute_partition
    settings:
      enable_hybrid: true
      slurm_cluster_name: $(vars.cluster_name)
      hybrid_conf:
        slurm_control_host: $(vars.onprem_ctld_host)
        slurm_control_addr: $(vars.onprem_ctld_addr)
        use_same_path_on_cloud: false
        output_dir: $(vars.output_dir)
        install_dir: $(vars.on_prem_install_dir)/etc
        slurm_uid: $(vars.slurm_uid)
        slurm_gid: $(vars.slurm_gid)
        slurm_log_dir: $(vars.on_prem_install_dir)/var/log
        slurm_bin_dir: $(vars.on_prem_install_dir)/bin
