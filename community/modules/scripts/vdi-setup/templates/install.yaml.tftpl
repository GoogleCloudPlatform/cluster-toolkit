# Generated by Cluster Toolkit

- hosts: localhost
  become: true
  vars_files:
    - "/opt/vdi-setup/vars.yaml"
  tasks:

%{ for r in roles }
    - name: Execute ${r} role
      ansible.builtin.import_role:
        name: "${r}"
%{ endfor }
