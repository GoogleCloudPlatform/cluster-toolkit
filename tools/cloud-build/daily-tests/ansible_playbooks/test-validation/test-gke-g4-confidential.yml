# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
- name: Assert variables are defined
  ansible.builtin.assert:
    that:
    - region is defined
    - custom_vars.project is defined

- name: Get cluster credentials for kubectl
  delegate_to: localhost
  ansible.builtin.shell: |
    gcloud container clusters get-credentials {{ deployment_name }} --region {{ region }} --project {{ custom_vars.project }} --verbosity=debug

- name: Run and verify G4 Confidential tests
  block:
  - name: Run the G4 Confidential Verification test
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl create -f {{ workspace }}/examples/gke-g4-confidential/g4-verification-test.yaml -v=9
    args:
      executable: /bin/bash

  - name: Wait for G4 Verification test to hit completion
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl wait --for=condition=complete job/g4-verification-test --timeout=1200s
    register: wait_result
    until: wait_result.rc == 0
    retries: 3
    delay: 10

  - name: Fetch logs from the verification pod
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl logs job/g4-verification-test
    register: verification_logs

  - name: Print the verification pod logs
    ansible.builtin.debug:
      msg: "{{ verification_logs.stdout }}"

  - name: Ensure test output contains SUCCESS
    ansible.builtin.assert:
      that: "'SUCCESS: G4 GPU computation completed successfully!' in verification_logs.stdout"
      fail_msg: "The Confidential Verification Test did not report SUCCESS."

  - name: Run the G4 Confidential Storage test
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl create -f {{ workspace }}/examples/gke-g4-confidential/g4-verification-storage-test.yaml -v=9
    args:
      executable: /bin/bash

  - name: Wait for Storage test to hit completion
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl wait --for=condition=complete job/g4-verification-storage-test --timeout=1200s
    register: wait_result
    until: wait_result.rc == 0
    retries: 3
    delay: 10

  - name: Fetch logs from the storage verification pod
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl logs job/g4-verification-storage-test
    register: storage_logs

  - name: Print the storage verification pod logs
    ansible.builtin.debug:
      msg: "{{ storage_logs.stdout }}"

  - name: Ensure storage test output confirms successful confidential volume read/write and GPU computation
    ansible.builtin.assert:
      that:
      - "'Confidential Data: G4 SEV CPU + G4 NVIDIA CC GPU' in storage_logs.stdout"
      - "'SUCCESS: G4 GPU computation completed successfully!' in storage_logs.stdout"
      fail_msg: "The Storage Verification Test did not verify both successful confidential volume read/write and GPU computation."

  - name: Run the G4 NVBandwidth test
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl create -f {{ workspace }}/examples/gke-g4-confidential/g4-verification-nvbandwidth.yaml -v=9
    args:
      executable: /bin/bash

  - name: Wait for NVBandwidth test to hit completion
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl wait --for=condition=complete job/g4-verification-nvbandwidth --timeout=1200s
    register: wait_result
    until: wait_result.rc == 0
    retries: 3
    delay: 10

  - name: Fetch logs from the nvbandwidth pod
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl logs job/g4-verification-nvbandwidth
    register: nvbandwidth_logs

  - name: Ensure nvbandwidth output confirms confidential encrypted bounce buffers
    ansible.builtin.assert:
      that:
      - "'Detected platform using encrypted bounce buffers for CPU<->GPU traffic.' in nvbandwidth_logs.stdout"
      - "'Waived host_to_device_bidirectional_memcpy_ce: bounce-buffer confidential computing enabled' in nvbandwidth_logs.stdout"
      fail_msg: "NVBandwidth test failed or did not run under bounce-buffer CC mode."

  rescue:
  - name: Capture K8s events for jobs, pods, and PVCs on failure
    delegate_to: localhost
    ansible.builtin.shell: |
      echo "=== JOB & POD DESCRIPTIONS ==="
      kubectl describe job g4-verification-test g4-verification-storage-test || true
      kubectl describe pods -l name=g4-verification-test || true
      kubectl describe pods -l name=g4-verification-storage-test || true
      echo "=== PVC STATUS ==="
      kubectl describe pvc || true
    register: failure_debug_info
    changed_when: false

  - name: Print detailed K8s failure diagnostics
    ansible.builtin.debug:
      msg: "{{ failure_debug_info.stdout_lines }}"

  - name: Fetch logs from verification pod on failure
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl logs job/g4-verification-test || true
    register: failed_verification_logs

  - name: Print verification logs on failure
    ansible.builtin.debug:
      var: failed_verification_logs.stdout_lines

  - name: Fetch logs from storage verification pod on failure
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl logs job/g4-verification-storage-test || true
    register: failed_storage_logs

  - name: Print storage logs on failure
    ansible.builtin.debug:
      var: failed_storage_logs.stdout_lines

  - name: Fail the playbook
    ansible.builtin.fail:
      msg: "G4 Confidential Verification tests failed."

  always:
  - name: Fetch logs and describe nvbandwidth pod on failure
    delegate_to: localhost
    ansible.builtin.shell: |
      echo "=== NVBANDWIDTH JOB & POD DESCRIPTION ==="
      kubectl describe job g4-verification-nvbandwidth || true
      kubectl describe pods -l name=g4-verification-nvbandwidth || true
      echo "=== NVBANDWIDTH LOGS ==="
      kubectl logs job/g4-verification-nvbandwidth || true
    register: failed_nvbandwidth_logs

  - name: Print nvbandwidth logs on failure
    ansible.builtin.debug:
      var: failed_nvbandwidth_logs.stdout_lines

  - name: Clean up
    delegate_to: localhost
    ansible.builtin.shell: |
      kubectl delete job g4-verification-test g4-verification-storage-test g4-verification-nvbandwidth --ignore-not-found -v=9
