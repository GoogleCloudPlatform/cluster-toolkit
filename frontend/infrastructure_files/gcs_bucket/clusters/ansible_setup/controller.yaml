---
  - name: "Controller Setup"
    fact_path: ./facts.d
    hosts: localhost
    connection: local
    vars_files:
      - ./vars.yaml
    roles:
      - spack_install
      - spack_setup
      # This one should be last - has a role to check in with FE to signal ready
      - c2_daemon
